<div class="container">
<h2>Edit Profile/Settings</h2>

<%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
    <%= f.input :email,
                required: true,
                autofocus: true,
                input_html: { autocomplete: "email" }%>
    <%= f.input :password,
                required: true,
                hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :password_Confirmation,
                required: true,
                input_html: { autocomplete: "new-password" } %>
    <%= f.input :name,
                required: true,
                input_html: { autocomplete: "name" } %>
    <%= f.input :address,
                required: true,
                input_html: { autocomplete: "Address" } %>
    <%# <%= f.input :alert_preference,
                required: true,
                collection: ["yes", "no"], 
                input_html: { autocomplete: "alert_preference" } %> 
    <div data-controller="alert-preference"> 
      <label for="preferences">Alert_preference</label>
    <select id="preferences" name="preferences" data-action="change->alert-preference#toggle">
      <option
      value="yes"
      >Yes</option>
       <option
      value="no"
      >No</option></select>
      <div data-alert-preference-target="inputs">
    <%= f.input :morning_alert,
                required: false,
                input_html: { autocomplete: "morning_alert" } %>
     <%= f.input :evening_alert,
                required: false,
                input_html: { autocomplete: "evening_alert" } %>
  </div>

  </div>
  </div>

  <div class="form-actions">
   <%= f.button :submit, "Cancel" %>
   </div>

  <div class="form-actions">
    <%= f.button :submit, "Save" %>
    </div>

  <div class="Privacy-policy">
    <p>By signing up to create an account I accept Company’s Terms of Use and Privacy Policy.</p>
    <p><string>Copyright 2024 Garbage Genius.  All rights reserved</p></strong>
<% end %>

<%= render "devise/shared/links" %>
</div>
</div>